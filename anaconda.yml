---
- name: install pyenv and miniconda 
  hosts: localhost
  gather_facts: true

  roles:
    - python
    - pyenv
    - role: userspace_applications
      vars:
        - list_userspace_applications:
          - "22-python-anaconda"
          - "999ready"
    - anaconda
