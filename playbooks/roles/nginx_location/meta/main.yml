---
galaxy_info:
  author: Dawa Ometto
  description: Add location blocks to the SRC nginx environment, including support for SRAM auth, basic auth, and reverse proxies. Builds on top of the SRC-Nginx component.
  license: GPLv3
  min_ansible_version: "2.9"
  platforms:
    - name: Debian
    - name: Ubuntu
dependencies:
  - require_src_nginx
