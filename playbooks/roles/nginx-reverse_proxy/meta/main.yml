---
galaxy_info:
  author: Dawa Ometto
  description: A role to add reverse proxy locations to the SRC nginx environment, including support for SRAM auth and basic auth. Builds on top of the SRC-Nginx component (https://gitlab.com/rsc-surf-nl/plugins/plugin-nginx).
  license: GPLv3
  min_ansible_version: '2.9'
  platforms:
    - name: Debian
    - name: Ubuntu
