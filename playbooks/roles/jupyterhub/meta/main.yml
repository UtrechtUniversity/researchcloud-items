---
galaxy_info:
  author: Dawa Ometto
  description: Install JupyterHub, by default with external user auth (SRAM).
  license: GPLv3
  min_ansible_version: "2.9"
  platforms:
    - name: Ubuntu
      versions:
        - all
dependencies:
  - role: system_python
  - role: nodesource
