---
- name: Reload jupyterhub service
  ansible.builtin.systemd:
    state: restarted
    daemon_reload: true
    enabled: true
    name: jupyterhub

- name: Restart nginx
  ansible.builtin.systemd:
    state: restarted
    name: nginx
