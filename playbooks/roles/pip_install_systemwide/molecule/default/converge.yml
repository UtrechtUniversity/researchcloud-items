---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: Testing pip_install_systemwide role
      include_role:
        name: pip_install_systemwide
      vars:
        pip_install_systemwide_packages: 'docker'
