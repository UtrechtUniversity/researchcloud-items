---
- name: Install iBridges and iCommands systemwide
  hosts: localhost
  gather_facts: true

  roles:
    - role: pip_install_systemwide
      vars:
        pip_install_systemwide_packages: ibridges
    - role: irods_repo
    - role: irods_icommands
    - role: irods_skel
