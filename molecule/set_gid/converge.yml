---
- name: Converge
  hosts: all
  gather_facts: true
  tasks:
    - name: Testing role set_gid
      ansible.builtin.include_role:
        name: set_gid
      vars:
        set_gid_paths: '/shared, /home/testuser/shared'
        set_gid_groupname: 'testgroup'