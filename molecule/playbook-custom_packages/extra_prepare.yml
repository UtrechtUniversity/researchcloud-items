---
- name: Extra prepare
  hosts: all
  gather_facts: false
  tasks:
    - name: Install jupyter (dependency of IRKernel)
      ansible.builtin.apt:
        name: jupyter
        install_recommends: false
        state: present
