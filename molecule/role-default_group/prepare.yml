---
# This playbook ensures that dependencies on the Ansible controller (localhost) are installed

- name: "Prepare dependencies on ansible controller"
  hosts: "localhost"
  tasks:
    # jmespath is required for filter 
    - name: ensure jmespath is installed
      pip:
        name: jmespath
