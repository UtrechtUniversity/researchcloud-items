---
- name: Install tweet collector and dependencies
  hosts: localhost
  gather_facts: true

  roles:
    # Dependency for tweet collector
    - docker
    - role: poetry
      vars: 
        poetry_system_wide: true
    - tweet_collector
...
