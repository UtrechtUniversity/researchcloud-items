---
- name: (Install system-wide) Start system-wide install play
  when: system_wide
  include: system_wide.yml

- name: (Install per user) Copy poetry installation script to runonce.d
  when: per_user
  copy:
    src: "poetry.sh"
    dest: "/etc/runonce.d"
    mode: 0755
...
