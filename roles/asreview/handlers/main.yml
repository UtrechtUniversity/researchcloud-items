---
- name: reload systemctl
  command: systemctl daemon-reload

- name: enable systemctl
  command: systemctl enable asreview.service

- name: unmask service
  command: systemctl unmask asreview.service

- name: systemctl actions
  systemd:
    name: asreview
    state: started
    enabled: yes
    masked: no

...
